# @5ive-tech/stdlib

Canonical standard library source package for 5IVE project scaffolding.

Current consumption mode:
1. `5ive init` vendors files from this package into `src/std`.

Future consumption mode:
1. direct dependency/linking once package/dependency resolution is ready.
