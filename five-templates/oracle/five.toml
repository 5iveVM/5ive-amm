# Oracle price feed adapter template

[project]
name = "five-oracle-template"
version = "1.0.0"
description = "Oracle price feed adapter template"
source_dir = "src"
build_dir = "build"
target = "vm"
entry_point = "src/main.v"

multi_file_mode = true

[optimizations]
enable_compression = true
enable_constraint_optimization = true
optimization_level = 2

[build]
max_bytecode_size = 2097152
target_compute_units = 500000
output_artifact_name = "five-oracle-template"

modules = [
    "src/types/oracle_types.v",
    "src/oracle/oracle_core.v",
    "src/main.v"
]

[deploy]
cluster = "devnet"
commitment = "confirmed"
rpc_url = "https://api.devnet.solana.com"

[doc]
generate_docs = true
docs_dir = "docs"
