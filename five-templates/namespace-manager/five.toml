[project]
name = "namespace-manager"
version = "0.1.0"
description = "5NS namespace registration and subprogram binding manager"
source_dir = "src"
build_dir = "build"
target = "vm"
entry_point = "src/main.v"

[build]
output_artifact_name = "namespace_manager"
