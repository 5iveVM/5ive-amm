[project]
name = "namespace-manager"
version = "0.1.0"
description = "5NS namespace registration and subprogram binding manager"

[build]
entry = "src/main.v"
output = "build/namespace_manager.five"

